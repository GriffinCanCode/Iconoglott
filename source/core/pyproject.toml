[project]
name = "iconoglott-core"
version = "0.1.0"
description = "Rust rendering core for Iconoglott"
requires-python = ">=3.11"

[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[tool.maturin]
features = ["pyo3/extension-module"]

