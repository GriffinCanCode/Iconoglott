// ═══════════════════════════════════════════════════════════════════════════
// GENERATED BY ts-rs - DO NOT EDIT
// Run `cargo test export_ts_bindings -- --ignored` in source/core to regenerate
// ═══════════════════════════════════════════════════════════════════════════

// ─────────────────────────────────────────────────────────────────────────────
// Lexer Types
// ─────────────────────────────────────────────────────────────────────────────

/**
 * Token types for lexical analysis
 */
export type TokenType = "Ident" | "Number" | "Percent" | "String" | "Color" | "Var" | "Pair" | "PercentPair" | "Size" | "Colon" | "Equals" | "Arrow" | "LBracket" | "RBracket" | "Newline" | "Indent" | "Dedent" | "Eof" | "AtKeyframes" | "Duration";

/**
 * Standard canvas sizes (10-tier system)
 */
export type CanvasSize = "Nano" | "Micro" | "Tiny" | "Small" | "Medium" | "Large" | "XLarge" | "Huge" | "Massive" | "Giant";

/**
 * Token value variants
 */
export type TokenValue = "None" | { "Str": string } | { "Num": number } | { "Pair": [number, number] } | { "PercentPair": [number, number] };

/**
 * A single token from the lexer
 */
export type Token = { ttype: TokenType, value: TokenValue, line: number, col: number, };

// ─────────────────────────────────────────────────────────────────────────────
// AST Types
// ─────────────────────────────────────────────────────────────────────────────

/**
 * Dimension (absolute or percent)
 */
export type Dimension = { "Abs": number } | { "Pct": number };

/**
 * Pair of dimensions
 */
export type DimensionPair = [Dimension, Dimension];

/**
 * Layout properties for flex containers
 */
export type LayoutProps = { direction: string, gap: number, padding: number, justify: string, align: string, wrap: boolean, };

/**
 * Animation state for a shape
 */
export type AnimationState = { name: string, duration: number, easing: string, delay: number, iteration: string, direction: string, fill_mode: string, };

/**
 * Symbol definition (reusable component)
 */
export type AstSymbol = { name: string, children: Array<AstShape>, };

/**
 * Symbol instantiation
 */
export type AstUse = { name: string, at: [number, number] | null, style: AstStyle, transform: AstTransform, animation: AnimationState | null, };

/**
 * Use element (symbol instance in scene)
 */
export type Use = { symbol_name: string, x: number, y: number, transform: string | null, style: ShapeStyle, };

/**
 * Single keyframe step
 */
export type KeyframeStep = { offset: number, properties: Record<string, unknown>, };

/**
 * Keyframes animation definition
 */
export type Keyframes = { name: string, steps: Array<KeyframeStep>, };

/**
 * Style properties for shapes
 */
export type AstStyle = { fill: string | null, stroke: string | null, stroke_width: number, opacity: number, corner: number, font: string | null, font_size: number, font_weight: string, text_anchor: string, };

/**
 * Shadow definition
 */
export type ShadowDef = { x: number, y: number, blur: number, color: string, };

/**
 * Gradient definition
 */
export type GradientDef = { gtype: string, from: string, to: string, angle: number, };

/**
 * Extended style with shadow/gradient (separate for Python compat)
 */
export type FullStyle = { base: AstStyle, shadow: ShadowDef | null, gradient: GradientDef | null, };

/**
 * Transform properties
 */
export type AstTransform = { translate: [number, number] | null, rotate: number, scale: [number, number] | null, origin: [number, number] | null, };

/**
 * Node definition for graphs/flowcharts
 */
export type GraphNode = { id: string, shape: string, label: string | null, at: [number, number] | null, size: [number, number] | null, style: AstStyle, };

/**
 * Edge/connector between nodes
 */
export type GraphEdge = { from: string, to: string, style: string, arrow: string, label: string | null, stroke: string | null, stroke_width: number, };

/**
 * Graph container with layout
 */
export type AstGraph = { layout: string, direction: string, spacing: number, nodes: Array<GraphNode>, edges: Array<GraphEdge>, };

/**
 * Canvas definition using standardized sizes
 */
export type AstCanvas = { size: CanvasSize, fill: string, };

/**
 * Property value types
 */
export type PropValue = "None" | { "Str": string } | { "Num": number } | { "Pair": [number, number] } | { "Points": Array<[number, number]> } | { "Dim": Dimension } | { "DimPair": DimensionPair } | { "PercentPair": [number, number] } | { "Layout": LayoutProps } | { "VarRef": [string, number, number] };

/**
 * Shape in the AST
 */
export type AstShape = { kind: string, props: { [key in string]?: PropValue }, style: AstStyle, shadow: ShadowDef | null, gradient: GradientDef | null, transform: AstTransform, animation: AnimationState | null, children: Array<AstShape>, };

/**
 * AST node types
 */
export type AstNode = { "Scene": Array<AstNode> } | { "Canvas": AstCanvas } | { "Shape": AstShape } | { "Graph": AstGraph } | { "Symbol": AstSymbol } | { "Use": AstUse } | { "Variable": { name: string, value: TokenValue | null, } } | { "Keyframes": Keyframes };

/**
 * Error severity levels
 */
export type ErrorSeverity = "Error" | "Warning" | "Hint";

/**
 * Error categories for structured diagnostics
 */
export type ErrorKind = "UnexpectedToken" | "UnknownCommand" | "InvalidValue" | "MissingToken" | "InvalidIndentation" | "UnterminatedBlock" | "InvalidProperty" | "UndefinedVariable" | "DuplicateVariable";

/**
 * Source span for error locations
 */
export type Span = { start_line: number, start_col: number, end_line: number, end_col: number, };

/**
 * Parse error with recovery context
 */
export type ParseError = { message: string, line: number, col: number, kind: ErrorKind, severity: ErrorSeverity, span: Span, suggestion: string | null, recovered: boolean, };

// ─────────────────────────────────────────────────────────────────────────────
// Scene Shape Types
// ─────────────────────────────────────────────────────────────────────────────

/**
 * RGBA color representation
 */
export type Color = { r: number, g: number, b: number, a: number, };

/**
 * Style properties for shapes
 */
export type ShapeStyle = { fill: string | null, stroke: string | null, stroke_width: number, opacity: number, corner: number, filter: string | null, 
/**
 * Animation class name (references CSS animation)
 */
animation_class: string | null, };

/**
 * Rectangle primitive
 */
export type Rect = { x: number, y: number, w: number, h: number, rx: number, style: ShapeStyle, transform: string | null, };

/**
 * Circle primitive
 */
export type Circle = { cx: number, cy: number, r: number, style: ShapeStyle, transform: string | null, };

/**
 * Ellipse primitive
 */
export type Ellipse = { cx: number, cy: number, rx: number, ry: number, style: ShapeStyle, transform: string | null, };

/**
 * Line primitive
 */
export type Line = { x1: number, y1: number, x2: number, y2: number, style: ShapeStyle, transform: string | null, };

/**
 * Path primitive
 */
export type Path = { d: string, style: ShapeStyle, transform: string | null, bounds_hint: [number, number, number, number] | null, };

/**
 * Polygon primitive
 */
export type Polygon = { points: Array<[number, number]>, style: ShapeStyle, transform: string | null, };

/**
 * Text primitive
 */
export type TextShape = { x: number, y: number, content: string, font: string, size: number, weight: string, anchor: string, style: ShapeStyle, transform: string | null, };

/**
 * Image primitive
 */
export type ImageShape = { x: number, y: number, w: number, h: number, href: string, transform: string | null, };

/**
 * Diamond primitive (rotated rect for flowcharts)
 */
export type Diamond = { cx: number, cy: number, w: number, h: number, style: ShapeStyle, transform: string | null, };

/**
 * Node for graph/flowchart (composite: shape + label)
 */
export type GraphNodeShape = { id: string, shape: string, cx: number, cy: number, w: number, h: number, label: string | null, style: ShapeStyle, label_style: ShapeStyle, transform: string | null, };

/**
 * Edge style enumeration
 */
export type EdgeStyle = "Straight" | "Curved" | "Orthogonal";

/**
 * Arrow type enumeration
 */
export type ArrowType = "None" | "Forward" | "Backward" | "Both";

/**
 * Edge/connector between nodes
 */
export type Edge = { from_id: string, to_id: string, from_pt: [number, number], to_pt: [number, number], edge_style: string, arrow: string, label: string | null, style: ShapeStyle, };

// ─────────────────────────────────────────────────────────────────────────────
// Scene Container Types
// ─────────────────────────────────────────────────────────────────────────────

/**
 * A renderable element in the scene
 */
export type Element = { "Rect": Rect } | { "Circle": Circle } | { "Ellipse": Ellipse } | { "Line": Line } | { "Path": Path } | { "Polygon": Polygon } | { "Text": TextShape } | { "Image": ImageShape } | { "Diamond": Diamond } | { "Node": GraphNodeShape } | { "Edge": Edge } | { "Group": [Array<Element>, string | null] } | { "Graph": GraphContainer } | { "Use": Use };

/**
 * Container for graph elements with layout info
 */
export type GraphContainer = { layout: string, direction: string, spacing: number, nodes: Array<GraphNodeShape>, edges: Array<Edge>, };

/**
 * Gradient definition
 */
export type GradientShape = { id: string, kind: string, from_color: string, to_color: string, angle: number, };

/**
 * Filter definition
 */
export type Filter = { id: string, kind: string, dx: number, dy: number, blur: number, color: string, };
